# 白色内网 Web 工作台 — 视觉基线资产（2026-07-27）

> **这批图不代表现状。** 2026-08-07 起界面改为**暗色默认 + 完整亮色**双主题，
> 结构、导航和术语沿用本目录，配色与质感已经换掉。
> 想看当前界面请去 [`docs/screenshots/`](../../screenshots/)；本目录作为设计过程留档保留，
> 下面「视觉方向已确认（白色 + 浅暖灰）」一行说的是当时，不是现在。

## 这批文件是什么

这是 GamePMer 内网 Web 工作台**唯一经过用户确认的 UI 规格来源**。

`docs/superpowers/specs/2026-07-27-intranet-web-workspace-redesign-design.md` 用文字定义业务、领域模型和状态机；
本目录用图定义布局、信息密度、组件形态和术语。两者是同一份设计的两个面，**实现时必须同时对照，不能只看文字**。

原始文件产自 `.superpowers/brainstorm/white-ui-architecture-20260727-v3/`，该目录被 `.gitignore` 忽略、
只存在于单台开发机、且配套预览服务会超时失效。2026-07-27 经用户授权复制到此处纳入版本控制，
避免重蹈 M1「只有文字规格、没有视觉基线」的覆辙（见 `docs/retrospectives/2026-07-16-m1-vibe-coding-retrospective.md` 根因一）。

## 状态

| 项 | 状态 |
|---|---|
| 视觉方向 | 已由用户确认（白色 + 浅暖灰分层，鼠尾草绿主色） |
| 页面结构 | 已由用户逐屏确认 |
| 书面设计说明 | 2026-07-27 审阅通过 |
| 阶段门 | 复盘流程的 S1（带示例数据的界面原型）已通过 |

## 目录

### `prototypes/` — 静态 HTML 原型

用浏览器直接打开即可查看，无需服务器、无外部依赖。

| 文件 | 对应模块 | 设计说明章节 | 截图 | 用户确认 |
|---|---|---|---|---|
| `white-reference-clone-home-v1.html` | 任务管理首页 | §6.1 | `01` | 已确认 |
| `schedule-capacity-page-v1.html` | 排期管理与团队档期 | §6.2 | `08` | 已确认（C3a 补） |
| `candidate-inbox-page-v1.html` | 候选收件箱 | §6.3 | `03` | 已确认 |
| `project-detail-gantt-page-v1.html` | 项目详情与完整甘特图 | §6.4 | `04` | 已确认 |
| `feedback-center-replan-page-v1.html` | 反馈中心与排期重排 | §6.5 | `05` | 已确认 |
| `quotation-and-change-page-v1.html` | 报价与变更 | §6.6 | `06` | 已确认 |
| `closeout-backup-billing-page-v1.html` | 结项、备份与出账 | §6.7 | `07` | 已确认 |
| `file-archive-page-v1.html` | 文件与归档 | §6.8 | `09` | **已作废**（见下） |
| `analytics-page-v1.html` | 智能分析 | §6.9 | `10` | **待确认** |
| `settings-page-v1.html` | 设置中心（含 LLM 供应商） | §6.10 / §3.2 | `11` | **待确认** |
| `business-line-and-ui-audit-v1.html` | 端到端业务线与 UI 自查 | §5 | `02` | 已确认 |
| `white-workspace-architecture-options-v1.html` | 信息架构方案对比（过程稿） | §4 | — | 过程稿 |

`08`–`11` 这四份是后补的，样式与前七份略有差异：它们是独立完整 HTML（自带设计令牌），
前七份是产自 `.superpowers` 的片段。**结构、导航、配色和术语一致**，差异只在文件形态。

### `screenshots/` — 审阅截图

`01`–`11` 前缀的 PNG 是各模块的审阅态截图（1920 宽），是最快的视觉参照。

曾经还有一组 `*-reference-*-comparison*.jpg`，是**用户参考图与本方案原型的并排对比**，
用于确认「忠实延续参考图的结构思路」这一约束有没有被稀释。
它们的左半幅是第三方商业产品的界面截图，**2026-08-12 已从全部 Git 历史中移除**
（见 [THIRD-PARTY-NOTICES.md](../../../THIRD-PARTY-NOTICES.md)），只留在维护者本机。
本目录里 `01`–`11` 前缀的截图都是本项目自己的原型，不受影响。

## 已作废的原型

`file-archive-page-v1.html` 把文件与归档设计成了**逐文件索引 + 阶段级关联**。
2026-07-28 用户验收时指出实际需要的是**按批次登记路径、可编辑、可一键跳转，且只挂项目不挂阶段**——
一个批次几十个资产，逐资产登记表根本没法看。

实现已按新方向重做，见实施计划 D6。**不要再按这份原型实现**，它只作为「走过的弯路」留档。

## 缺口状态

十个导航模块现已全部有原型。其中三份（文件与归档、智能分析、设置中心）**尚未经用户确认**，
在确认前不得进入对应模块的编码——这是复盘流程 S1 阶段门的硬要求。

## 使用规则

1. **原型定结构，不定实现。** 忠实复刻布局、信息层级、密度和术语，但用真实的 React 组件、
   设计令牌和响应式布局实现，不允许把静态 HTML 整体硬编码进组件文件。
2. **原型里的数据是虚构示例，不是种子数据规格。** 正式种子数据以设计说明 §11 为准。
3. **修改视觉基线需要用户重新确认。** 本目录不随实现自由演进；若实现中发现原型有问题，
   先提出、确认后再更新此处文件，并在提交信息中说明变更原因。
4. 正文实现字号不小于 14px；状态不能只用颜色编码；1280 / 1440 / 1920 三档都要检查。
