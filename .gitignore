# 目录名一律用 `/` 开头锚定到仓库根。
# 不锚定的 `data/` 会匹配任意层级的同名目录——它曾经把整个
# `apps/gamepmer-web/src/data/` 忽略掉，四个源文件因此一直没入库，
# 而 git status 是干净的，所以没人发现。写新规则前先想清楚要不要递归。

# Visual Studio and .NET build output
/.vs/
/.worktrees/
**/bin/
**/obj/
/TestResults/
/artifacts/

# Local brainstorming and design previews
/.superpowers/

# 第三方产品界面截图：只留在本机作设计过程参考，不进版本库。
# 这些图的左半幅是别家商业产品的完整界面，仓库以 MIT 发布，
# 把它们放进来等于替别人的作品做了不属于我们的授权。
# 2026-08-12 已从全部历史中移除；这条规则防止它们再被 `git add -A` 带回去。
docs/design-assets/**/screenshots/*-reference-*-comparison*.jpg

# GamePMer runtime data（只在仓库根，不要连累源码目录）
/data/
/backups/
/logs/
/cache/
/secrets/
*.db
*.db-shm
*.db-wal
*.key
*.log

# Local configuration and credentials
.env
.env.*
!.env.example
appsettings.Local.json
*.pfx
*.snk

# User-specific files and installers
*.user
*.suo
*.msix
*.msixbundle
*.appinstaller
